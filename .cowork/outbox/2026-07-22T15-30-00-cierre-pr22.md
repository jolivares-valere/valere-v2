# Cierre PR-2.2 (2/5 semana 2) — 2026-07-22

## PR-2.2 — MERGEADO (#73, squash 319ee6e)
- RenovacionesPage sobre `useListParams`: búsqueda (debounce 300ms), orden (SortableTh core), filtros — TODO en URL compartible (`?q=&prioridad=&cia=&mes=&estado=&sort=&dir=`).
- Filtros-chip: Prioridad (5 chips toggle) y Comercializadora (chips derivadas de los datos, orden alfabético); Mes de vencimiento (desplegable, solo meses con datos) y Estado (desplegable). Combinables entre sí y con la búsqueda.
- Export Excel: `fetchRows={() => Promise.resolve(lista)}` — exporta el conjunto FILTRADO completo en el orden visible. Antes re-consultaba el servidor solo con estado/prioridad (habría ignorado búsqueda/cia/mes). `fetchRenovacionesForExport` queda sin uso en esta página (no borrada de api.ts).
- Nota de caducidad: comentario junto a `LISTA_PAGE_SIZE` + entrada en `docs/MEJORAS_UI_BACKLOG.md` (dormida hasta ~2.000-3.000 filas vivas; revisar en gates).
- Doc RAG: `gestionar-renovaciones.md` sección "Filtros-chip y export a Excel".

## Cambios intencionados (para el paseo)
1. Filtros estado/prioridad: de server-side a client-side sobre el conjunto COMPLETO. Arregla inconsistencia latente: `cupsStatus` (Vigente/Histórico) se calculaba sobre el conjunto ya filtrado; ahora siempre sobre el total cargado.
2. "Limpiar" resetea también el orden (vuelta íntegra a la vista por defecto), y vacía el cuadro de búsqueda (estado local controlado).
3. Búsqueda a la URL con debounce 300ms cancelable (garantía Empresas: 1 escritura por pausa ≥300ms).
4. Orden por defecto explícito: vencimiento desc (= el orden que ya servía el servidor).

## Guion de paseo sugerido (CA)
- Chip Crítica + chip NEXUS + mes jul → filas correctas; con PR-2.4 (tarjeta Críticas clicable) quedará en 2 clics exactos.
- Export con filtros activos → .xlsx abre en Excel con TODAS las filas filtradas (no una página) y cifra igual al contador en pantalla.
- Mes con vencidas: filtrar un mes pasado con críticas vencidas (PAZ Y BIEN) → siguen visibles.
- URL con chips activos abierta en pestaña nueva → restaura estado exacto.
- Regresión: KPIs intactos (siguen siendo v_renovaciones_kpi, "tarjetas = SQL del día"), badge Vigente/Histórico con los mismos casos de PR-1.3.

## Verificación Juan
- tsc 0 (confirmado tras push) · 195 tests · git status solo 3 ficheros · commit a2e66aa · squash #73.

## PENDIENTE
- [x] Paseo del auditor: PASA (22-jul, actas en Drive). Derivadas: cache-busting (backlog S) + 4ª congelación renderer (expediente PR-4.3, profile barato en PR-2.3 si sale gratis).
- [ ] PR-2.3 Contratos sobre el componente único (comercializadora+estado combinables, CA del plan).
- [ ] Luego PR-2.4 KPIs clicables (+ "⚠ N vencidas") y PR-2.5 bandeja sin fecha (+ "— · N sin fecha").

GATE V2 (viernes 24-jul): 3 preguntas de negocio de Juan desde la UI en <30s, cronometradas.
