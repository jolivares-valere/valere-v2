# MCP Setup — handoff a Claude Code CLI

**De:** Cowork (sesión 2026-04-23)
**Para:** Claude Code CLI (Agente 2)
**Asunto:** Montar GitHub / Supabase / Vercel MCP en el entorno local de Juan.

## Qué dejo hecho
- `.mcp.json` nuevo en la raíz con los 3 servidores (supabase, github, vercel).
- `docs/MCP_SETUP.md` con la guía completa (PATs, scopes, verificación).

## Qué necesito que hagas tú
En la terminal de Juan:

```bash
cd ~/valere-v2
git checkout -b claude/mcp-setup
git add .mcp.json docs/MCP_SETUP.md .cowork/outbox/2026-04-23T07-22-06-mcp-setup.md
git commit -m "feat(mcp): configurar .mcp.json con supabase, github, vercel"
git push -u origin claude/mcp-setup
gh pr create --title "feat(mcp): configurar MCPs (supabase, github, vercel)" --body "Ver docs/MCP_SETUP.md"
```

Antes de considerar la tarea cerrada, verifica:
1. Juan ha creado `SUPABASE_ACCESS_TOKEN` y `GITHUB_PERSONAL_ACCESS_TOKEN` en `.env`.
2. `claude mcp list` muestra los 3 como `connected`.
3. Prueba real: `list_projects` contra Supabase devuelve `gtphkowfcuiqbvfkwjxb`.
4. Prueba real: `list_projects` contra Vercel devuelve el proyecto `valere-v2`.
5. Prueba real: list últimos 3 PRs de `jolivares-valere/valere-v2` via GitHub MCP.

## Nota sobre el working tree
El working tree en Cowork muestra ~150 ficheros modificados, muy probablemente por diferencias CRLF/LF entre el mount de Cowork y el clone de Windows. **No he tocado ninguno**; sólo añadí ficheros nuevos. Revisa `git status` en tu lado antes de añadir.

## Siguiente paso natural (no crítico hoy)
Con los MCPs conectados, el siguiente sprint lógico es ejecutar la migration pendiente `supabase/migrations/20260422_fase28_6_rls_policies_cleanup.sql` usando Supabase MCP directamente (sin copiar/pegar SQL en el dashboard).
