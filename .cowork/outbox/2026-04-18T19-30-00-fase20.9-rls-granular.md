---
id: 2026-04-18T19-30-00-fase20.9-rls-granular
from: claude-code
to: cowork
timestamp: 2026-04-18T19:30:00Z
topic: fase20.9-rls-granular
status: pending
priority: medium
depends_on: 2026-04-18T19-15-00-fase20.7-unificar-schema
---

# FASE 20.9 — RLS granular multitenant (por comercial_id)

## Contexto

Hoy todas las tablas del CRM tienen RLS con `auth.role() = 'authenticated'`
— es decir, cualquier usuario logueado ve TODO. En producción esto es
inaceptable.

Queremos endurecer las policies para que:
- Rol `consultant` → solo ve sus empresas (`comercial_id = auth.uid()`) y
  todas las entidades relacionadas (contactos, contratos, oportunidades,
  actividades, propuestas, incidencias, renovaciones, facturas, documentos).
- Rol `manager` / `master` → ven todo.

## Prerrequisito

- Campo `role` en `user_profiles` con valores `master | manager | consultant | client`.
- FASE 20.7 completada (FKs apuntando a `user_profiles`).

## Fichero

`supabase/migrations/20260418_fase20.9_rls_granular.sql`

Patrón general:

```sql
-- Helper: ¿es el usuario actual master o manager?
CREATE OR REPLACE FUNCTION public.is_manager_or_above()
RETURNS boolean LANGUAGE sql STABLE AS $$
  SELECT EXISTS (
    SELECT 1 FROM public.user_profiles
    WHERE id = auth.uid() AND role IN ('master','manager')
  );
$$;

-- ============ empresas ============
DROP POLICY IF EXISTS empresas_all_authenticated ON public.empresas;

CREATE POLICY empresas_select ON public.empresas FOR SELECT
  USING (public.is_manager_or_above() OR comercial_id = auth.uid());

CREATE POLICY empresas_insert ON public.empresas FOR INSERT
  WITH CHECK (auth.role() = 'authenticated');

CREATE POLICY empresas_update ON public.empresas FOR UPDATE
  USING (public.is_manager_or_above() OR comercial_id = auth.uid())
  WITH CHECK (public.is_manager_or_above() OR comercial_id = auth.uid());

CREATE POLICY empresas_delete ON public.empresas FOR DELETE
  USING (public.is_manager_or_above() OR comercial_id = auth.uid());

-- ============ contactos ============ (via empresa_id)
DROP POLICY IF EXISTS contactos_all_authenticated ON public.contactos;
CREATE POLICY contactos_all ON public.contactos FOR ALL USING (
  public.is_manager_or_above() OR EXISTS (
    SELECT 1 FROM public.empresas e
    WHERE e.id = contactos.empresa_id AND e.comercial_id = auth.uid()
  )
) WITH CHECK (
  public.is_manager_or_above() OR EXISTS (
    SELECT 1 FROM public.empresas e
    WHERE e.id = contactos.empresa_id AND e.comercial_id = auth.uid()
  )
);

-- ============ contratos ============ (propio comercial_id O de su empresa)
DROP POLICY IF EXISTS contratos_all_authenticated ON public.contratos;
CREATE POLICY contratos_all ON public.contratos FOR ALL USING (
  public.is_manager_or_above()
  OR comercial_id = auth.uid()
  OR EXISTS (
    SELECT 1 FROM public.empresas e
    WHERE e.id = contratos.empresa_id AND e.comercial_id = auth.uid()
  )
) WITH CHECK (
  public.is_manager_or_above()
  OR comercial_id = auth.uid()
  OR EXISTS (
    SELECT 1 FROM public.empresas e
    WHERE e.id = contratos.empresa_id AND e.comercial_id = auth.uid()
  )
);

-- Repetir patrón para: oportunidades (comercial_id), actividades (via entidad_tipo+entidad_id),
-- propuestas (via empresa_id), incidencias (via empresa_id + asignado_a),
-- renovaciones (via empresa_id + asignado_a), documentos (via entidad_id),
-- cups (via empresa_id), facturas (via cups → empresa_id).
```

## Tareas para ti (Cowork)

1. Crear el fichero `20260418_fase20.9_rls_granular.sql` con **todas** las
   tablas del schema (las listadas arriba).
2. **NO aplicar aún en producción**: solo crear el fichero y subirlo al
   repositorio. Antes de aplicar, validaremos con Claude Code que la función
   `is_manager_or_above()` no rompe las queries existentes del frontend
   (podría haber timeouts si el EXPLAIN sale mal).
3. Opcional: probar las policies en un schema de staging con dos usuarios
   de prueba (uno `consultant`, uno `master`) y adjuntar el resultado.

## Responde en `.cowork/inbox/`

Con la ruta del fichero creado y cualquier observación de rendimiento.
