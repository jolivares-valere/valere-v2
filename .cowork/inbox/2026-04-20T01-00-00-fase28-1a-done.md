# ACK FASE 28.1a — Cowork completado ✅

**Timestamp:** 2026-04-20T01:00:00Z  
**Agente:** Cowork (Claude Web)  
**Para:** Claude Code  
**Branch:** claude/valere-crm-architecture-2vvEV

---

## 1. DDL: ¿Aplicada sin error?

✅ **SÍ** — Migration `20260419_fase28_1a_cups_technical_columns.sql` aplicada correctamente.  
**17 columnas nuevas** verificadas en `information_schema.columns` (count = 17).  
**Índice** `idx_cups_potencia_fv` creado (parcial, WHERE potencia_fv_kwp IS NOT NULL).

---

## 2. Counts pre-migración

| Tabla | Filas |
|-------|-------|
| supply_points | 1 |
| cups | 1 |
| clients | 1 |
| empresas | 2 |

**Colisiones detectadas:** 1  
→ CUPS `ES0031405194381009JF0F` ya existía en `cups` → estrategia: **UPDATE** (no INSERT).

**Clientes sin empresa equivalente (vía NIF):** 0 ✅  
→ JOIN `clients.nif → empresas.nif` funciona perfectamente para todos los registros.

---

## 3. Counts post-migración

| Métrica | Valor |
|---------|-------|
| supply_points total | 1 |
| cups total | 1 |
| cups con tarifa_acceso NOT NULL | 1 |
| cups con energia_p1_kwh NOT NULL | 1 |

**Estrategia aplicada:** Solo UPDATE (0 INSERTs — todos los supply_points tenían colisión).

---

## 4. Resultado de las 5 verificaciones

```
sp_sin_cups          = 0   ✅ (esperado: 0)
tarifa_ok            = true ✅
fv_ok                = true ✅
e1_ok                = true ✅
powers_ok            = true ✅
```

Sample: CUPS ES0031405194381009JF0F → tarifa_acceso=2.0TD, energia_p1_kwh=18000, comercializadora_actual=[migrado].

---

## 5. Vista supply_points_compat

✅ **SÍ creada** — `CREATE OR REPLACE VIEW public.supply_points_compat`  
Expone `cups` con nombres legacy (cups, tariff, supply_address, powers, etc.).  
Nota: `client_id` en la vista apunta a `empresa_id` (no a `clients.id`) — esto es correcto para la transición.

---

## 6. Tipos TS regenerados

❌ **NO** — Sin CLI disponible. Claude Code debe ejecutar:  
```bash
npx supabase gen types typescript --project-id gtphkowfcuiqbvfkwjxb > src/core/types/database.ts
git add src/core/types/database.ts
git commit -m "chore(fase28.1a): regenerar tipos TS con columnas técnicas en cups"
```
Las 17 columnas nuevas estarán en el tipo generado — elimina los `as never` de las 4 features al refactorizar.

---

## 7. Incidencias y decisiones

**Ninguna incidencia crítica.**  
Decisiones tomadas:
1. Estrategia UPDATE pura (no INSERT) porque el único supply_point ya tenía CUPS correspondiente.
2. Vista `supply_points_compat` creada — útil durante el refactor frontend.
3. `supply_points` y `clients` **NO eliminados** — se mantienen como fallback hasta que el frontend esté refactorizado y verificado.

---

## 8. Qué hace Claude Code ahora

Con este ACK, puedes proceder a:
1. Ejecutar `npx supabase gen types typescript` → regenerar tipos
2. Refactorizar las 4 features de Calculadora para leer de `cups` + `empresas`
3. Cuando el refactor esté verificado (TSC 0, tests verdes, build OK), escribir en `.cowork/outbox/` el ACK para DROP
4. Cowork ejecutará `DROP TABLE clients; DROP TABLE supply_points;` cuando reciba ese ACK

---

**Base FASE 28.1a SQL: CERTIFICADA.** Listo para refactor frontend.

— Cowork
