# Handoff sesión Claude Code 2026-04-20 (tarde)

> Para Jesús — cuando vuelvas mañana (2026-04-21).
> Rama: `claude/valere-crm-architecture-2vvEV`
> PR #1 actualizado.

## TL;DR

Trabajo autónomo tras los re-test de Cowork. El cableado de automatizaciones tenía un bug sutil que Cowork detectó: el form de oportunidades ofrecía **dos** opciones etiquetadas "Ganada" (canónica y legacy) y solo la canónica disparaba el borrador de contrato. Fix aplicado + limpieza del form. También añadí CI, logger más robusto y 11 tests más.

## Commits de hoy (tarde)

| Commit | Qué hace |
|--------|----------|
| `2df6d7e` | Fix automatización ganada→contrato con `canonicalEtapa` + logger `serializeError` (elimina `[object Object]`) + 11 tests nuevos (17→28) |
| `29d2eff` | Eliminar etapas legacy del select de OportunidadForm; `normalizarEtapa()` convierte legacy→canónica al cargar |
| `c9e2594` | CI de GitHub Actions (tsc + test + build en cada push) + actualizar ESTADO.md |
| `a40634e` | Persistir fix `get_user_rol()` (master/manager → admin) en la migration SQL de Cowork |

## Estado de verificación

| Bug | Estado | Notas |
|-----|--------|-------|
| BUG 1 — custom_fields INSERT 404 | ✅ Resuelto con SQL | Cowork aplicó la migration, funcionan CRUD |
| BUG 2 — slug solo primera letra | ✅ Verificado por Cowork | `sector_empresa` se genera bien |
| BUG 3 — POST /empresas 404 | ✅ Resuelto con SQL | FKs recreados a user_profiles |
| BUG 4 — `users_profile` does not exist | ✅ Resuelto con SQL | FKs apuntan a user_profiles |
| BUG 5 — /admin directo redirige | ✅ Verificado por Cowork | profileLoaded flag funciona |
| Encoding `â€"` | ✅ Verificado por Cowork | Todos los mojibake corregidos |
| **Automatización ganada→contrato** | ⏳ **Pendiente re-test** | Fix canonicalEtapa aplicado. Cowork no pudo en viewport 533px por drag&drop; con el commit 29d2eff el form ya solo ofrece etapas canónicas, debería funcionar via `onSubmit` o via drag&drop indistintamente. |
| Automatización contrato→tarea | ⏳ Pendiente | Depende de la anterior |

## Qué tienes que hacer mañana

### 1. Aplicar la migration actualizada en Supabase

Está en `supabase/migrations/20260420_fase28_2_fixes_rls_fks.sql`. Cowork ya aplicó las partes 1, 2 y la parte 3 (get_user_rol) manualmente. Si vuelves a ejecutarla entera es idempotente (no rompe nada).

### 2. `git pull` y re-test

```powershell
cd C:\Users\joliv\valere-v2
git pull origin claude/valere-crm-architecture-2vvEV
```

Vite hot-reload debería coger todo.

### 3. Verificar los 2 bugs pendientes

En `http://localhost:3000`:

**Test A — Ganada → Contrato vía modal de edición** (este es el camino que falló en el re-test de Cowork):

1. Oportunidades → abrir cualquiera → Editar
2. Cambiar "Etapa" a **"Ganada"** (el select ya solo tiene las canónicas; no hay confusión)
3. Guardar
4. Debe aparecer toast "Borrador de contrato creado para..."
5. Ve a `/contratos` → debe haber un contrato nuevo con `compania="Pendiente"`

**Test B — Contrato activo → Tarea** (depende de A):

1. Abrir el borrador creado en A
2. Cambiar estado a "activo"
3. Guardar
4. Toast "Tarea de seguimiento creada"
5. Ve a `/actividades` → tarea nueva con fecha vencimiento 30 días desde hoy

## Pendientes NO bloqueantes

| Tarea | Cuándo |
|-------|--------|
| Deploy Edge Function `chat-consultor` | Cuando tengas CLI Supabase |
| Secrets `GEMINI_API_KEY`, `ALLOWED_ORIGIN` | Cuando tengas acceso al dashboard Supabase |
| DROP `clients` + `supply_points` | Cuando Cowork confirme `facturas.cups_id IS NULL = 0` |
| Regenerar tipos TS | Cuando haya `SUPABASE_ACCESS_TOKEN` en el entorno |

## Revisión de diseño

Lancé un subagente en background para hacer una revisión UX/consistencia/accesibilidad completa. El informe se guarda en `docs/DESIGN_REVIEW_2026-04-20.md` cuando termine. Búscalo al volver; contendrá hallazgos priorizados por valor/coste que podemos ir aplicando uno a uno.

## Arquitectura: tú vs yo vs Cowork

- **Tú**: Windows PowerShell, dev server `npm run dev` en localhost:3000, testing manual en el navegador.
- **Yo (Claude Code)**: sandbox Linux remoto, edito código, commit + push al remoto. No tengo acceso a tu máquina Windows.
- **Cowork (Claude Web)**: testing en navegador, acceso a Supabase SQL Editor, reports técnicos.

El flujo hoy funcionó así: yo pusheo → tú `git pull` → Cowork testea → yo corrijo → ciclo.

## Commits totales de la sesión (mañana + tarde 2026-04-20)

```
a40634e chore(fase28.2): persistir fix get_user_rol() en migration
c9e2594 chore(ci): GitHub Actions + actualizar ESTADO.md
29d2eff refactor(fase28-4): eliminar etapas legacy del form
2df6d7e fix(fase28-3): canonicalEtapa + logger + tests
be8585b fix(fase28-2): bugs tras prueba end-to-end
bb0357a docs: actualizar ESTADO.md — FASE 28 completa
f51bfc8 feat(fase28-hardening): role gating admin, focus trap, CSP
abff85a feat(fase28-eje3): automatizaciones CRM
4aaf82f feat(fase28-eje2): dashboards por rol
f28d9a3 feat(fase28-eje1): custom fields en empresas y oportunidades
```
