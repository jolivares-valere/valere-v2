# Sesión 2026-07-16 — Día 0 del mes "CRM ÚTIL": limpieza + plan

## Qué se hizo

1. **Working tree a cero** (pendiente desde 06-jul):
   - Paquete SIPS F1 (src/features/sips/, EF resolver-sips-cups, doc RAG buscador-cups, cableado Sidebar+permissions) → commit `dff0ece` en rama `claude/f1-sips-cups` (actualizada a main y pusheada). La ruta en App.tsx se recableará al trabajar el PR.
   - Docs a main (`5001b2f`): MEJORAS_UI_BACKLOG.md al día (PR #59, canales resuelto, 4 tareas renovaciones), PLAN_FASE1_2_CANALES.md, PLAN_RENOVACIONES_DEDUP.md, resumen 2026-07-03, 4 outbox jul 05-07, _DRAFT_20260706_fase1_2_canales.sql.
   - Borrados: APLICAR_F1_SIPS_BUSCADOR.ps1, APLICAR_UI_EMPRESAS_V1.ps1, APLICAR_UI_EMPRESAS_V1_1.ps1 (ya cumplieron).

2. **Poda de ramas locales: de ~74 a 4.** Método: `--merged` (10), upstream "gone" tras prune (5), `git cherry` a 0 equivalencias (35), verificación contra ESTADO/producción para las 21 restantes con confirmación de Juan. Quedan: `main`, `claude/energia-v2-s0`, `claude/f1-sips-cups`, `claude/holded-integration` (única con trabajo único no mergeado — Holded Fase 0+1 abril, se conserva para el módulo comisiones). También borrada `backup_main_pre_reconcile_20260427`.

3. **Plan del mes**: `docs/PLAN_CRM_UTIL_4SEMANAS.md` (`6ef9de3`) desde el doc de Drive v2 2026-07-16.

4. **Entregables junio**: `docs/analisis_plataformas_junio/` con DOC1_COMPARATIVO, DOC2_MEJORAS_PRIORIZADAS, DOC4_ROADMAP y PLAN_TELEMEDIDA_FV_DIRECTA (`d3e27d4`).

## Decisiones

- `claude/holded-integration` se conserva (trabajo único, requisito futuro comisiones; rescate por cherry-pick cuando toque).
- SQL de canales commiteado con prefijo `_DRAFT` (no aplicar).
- Ramas borradas recuperables ~90 días por reflog; varias siguen en origin.

## Pendiente

- Cruzar DOC2 (mejoras priorizadas) con MEJORAS_UI_BACKLOG.md — única re-priorización admitida (semana 1 del plan).
- Arrancar PR-1.1 (cabecera ficha de empresa 360º).
- Subir los 4 entregables también a Drive (el plan pide Drive/repo; repo hecho).
