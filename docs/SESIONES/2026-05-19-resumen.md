# Sesión 2026-05-19 — Sprint Carolina (Hallazgos #2 + #3)

**Quién:** Juan + Cowork (en vivo, sin Code).
**Tipo:** revisión usuaria real (Carolina Aroca) → planteamiento UX → implementación backend completa → frontend base.

---

## Qué se hizo

### Fase 1 — Captura de fricciones (Carolina presente)

Carolina Aroca empezó a usar `/captacion` y dictó 3 fricciones:

1. **Sincronización agenda** — usa Outlook, ya migrados a Google Workspace. Quiere ver lo del CRM en su agenda.
2. **Buscador + pestaña de seguimiento de envíos** — "cuando una empresa ya ha mandado factura quiero verla en una pestaña", "seguimiento de propuestas enviadas a gestor o Carolina M".
3. **Visualización tipo Excel con filtros desplegables** — "todas las llamadas y propuestas, incluso cerradas, poder filtrar el listado".

Para la #3 se mostró mockup HTML en chat con la tabla, chips de filtros, edición inline y paginación. UX aprobada por Carolina+Juan.

Decisiones de Juan (firmadas en chat):
- Una pestaña Enviados con sub-chips Análisis/Senior (siempre desde historial unificado).
- Recordatorio = notificación CRM + email Resend.
- SLA 3d amarillo / 5d rojo.
- Pestaña "Seguimientos" actual eliminada; `propuesta_enviada` vuelve a "Por llamar".
- Edición inline propagable, en celdas seguras.
- Paginación incremental con prompt al llegar a 200.
- Filtros visuales chips horizontales + "Más filtros" desplegable.

### Fase 2 — Backend implementado y aplicado en prod

`supabase/migrations/20260519_sprint_vista_tabla_captacion.sql`:

| Pieza | Propósito |
|---|---|
| `v_captacion_historico_completo` | Tabla full para tab Histórico (modo Tabla). Todas las oportunidades del user + campos derivados (dias_vencimiento, dias_sin_movimiento) |
| `v_captacion_enviados_en_seguimiento` | Pestaña Enviados con `tipo_destinatario` (analista/senior) + `sla_color` (verde/amarillo/rojo) |
| `v_mis_llamadas` | Log cronológico actividades tipo `llamada` |
| `editar_campo_oportunidad(id, campo, valor)` | Edición inline whitelist 7 campos seguros |
| `editar_campo_empresa(id, campo, valor)` | Edición inline whitelist 7 campos seguros |
| `recordar_a_responsable(id, mensaje)` | Crea notificación CRM + actividad timeline, devuelve datos para email |

Edge Function `enviar-recordatorio` (deployada):
- Patrón idéntico a `notify-admin-pending-user`.
- Provider Resend con `noreply@valereconsultores.com`.
- JWT verify obligatorio.

Aplicación bloque a bloque vía SQL Editor de Supabase Dashboard (la migración total tenía problema de codificación al pegarse junta).

### Fase 3 — Frontend base implementado (commit pendiente)

`src/features/captacion/api.ts`:
- Hooks: `useCaptacionHistorico(filters)`, `useCaptacionEnviados()`, `useMisLlamadas(filters)`, `useEditarCampoOportunidad()`, `useEditarCampoEmpresa()`, `useRecordarAResponsable()`.
- Invalidación en cascada para que la edición inline propague visualmente.
- Cast `(supabase as any)` temporal en vistas/RPCs nuevas hasta regenerar tipos.

Componentes en `src/features/captacion/components/`:
- `SelectorVista.tsx` — dropdown Fichas/Tabla con persistencia localStorage.
- `BuscadorCaptacion.tsx` — input search con icono y X de limpiar.
- `CeldaEditable.tsx` — celda con edición inline (doble click, Enter/Esc/Blur).
- `ChipsFiltros.tsx` — chips horizontales con contadores + helper `aplicarChips`.
- `PaginacionIncremental.tsx` — "Has visto 200 de 543" con [Cargar más] [Cargar todas].
- `TablaCaptacion.tsx` — tabla con sort por columnas, export Excel, integrada con CeldaEditable.

Util `src/core/utils/exportToExcel.ts` — wrapper SheetJS para columnas formateables.

### Bonus — fix TSC preexistente

`DashboardPage.tsx` tenía la función `KPI` reemplazada por `PrecioPoolCard` en un sprint anterior, pero el componente quedó truncado y el reemplazo no se completó. Resultado: TSC roto en `main` desde hace tiempo. Sin esto, no se podía commitear nada.

Acción tomada:
- Eliminado `import` de `Zap` (no usado).
- Eliminado `import` de `usePrecioPool` (hook que no existe en `api.ts`).
- Eliminada línea `const precioPool = usePrecioPool()`.
- Eliminado render `<PrecioPoolCard ... />`.
- Eliminado componente truncado `PrecioPoolCard` al final.
- Reintroducida la función `KPI` original desde commit `0c6eea2`.

Resultado: **TSC vuelve a 0 errores**.

El sprint OMIE puede retomarse desde git history cuando se quiera.

---

## Qué quedó pendiente para próxima sesión

1. Componente `MisLlamadasView` con filtros (rango fechas / resultado / solo mías).
2. Integración `CaptacionPage.tsx`:
   - Toggle Vista (selector Fichas/Tabla).
   - Buscador inline arriba.
   - Chips de filtros sobre la tabla.
   - Nueva pestaña "Enviados" con sub-chips Análisis/Senior.
   - Eliminar pestaña actual "Seguimientos".
   - Nueva pestaña "Mis llamadas" o subapartado.
   - Renombrar "Todos mis casos" → "Histórico".
3. Componente `BandejaEnviadosCard` con badge SLA + botón "Recordar a responsable".
4. Modal "Recordar a responsable" con textarea y disparo de `useRecordarAResponsable`.
5. Tests:
   - CeldaEditable (Enter, Esc, error).
   - TablaCaptacion (sort, paginación).
   - Filtros chips.
6. Regenerar tipos TS Supabase: `npx supabase gen types typescript --project-id gtphkowfcuiqbvfkwjxb > src/core/types/database.ts`. Después limpiar `(supabase as any)` en `api.ts`.

---

## Decisiones importantes tomadas

- Pestaña "Seguimientos" actual eliminada del módulo Captación. Los `propuesta_enviada` se gestionan en "Por llamar" con siguiente acción "Llamar para seguimiento — propuesta enviada hace Xd".
- Recordatorio bidireccional (CRM + email) en lugar de sólo notificación. Acepta el coste de cuota Resend (100/día Free, sobra).
- SLA calibrado en 3d amarillo / 5d rojo — recalibrable más adelante.
- Sincronización Google Calendar (Hallazgo #1) postergada a sprint propio. Requiere setup OAuth en Google Cloud Console.

---

## Refs

- Mockup UX visual mostrado a Juan en chat (ver tool call `captacion_vista_tabla_mockup`).
- Archivo de captura: `~/.claude/2026-05-19-correcciones-carolina-EN-VIVO.md` (volcable a `docs/FEEDBACK_USO_REAL.md` cuando se quiera).
- Dossier de soporte: `docs/SESIONES/2026-05-19-revision-carolina-captacion-DOSSIER.md`.
- Script commit: `COMMIT_SPRINT_CAROLINA_2026-05-19.ps1`.
