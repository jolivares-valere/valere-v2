# Sesión 2026-05-11 — CI Fix + Diagnóstico FV Sync + Commit Módulo FV

## Qué se hizo

### 1. Fix crítico: workflow fv-sync.yml truncado
- Detectado que `.github/workflows/fv-sync.yml` estaba truncado en línea 63 (el step "Ejecutar sincronización" solo tenía `name:` — sin `run`, `env` ni `working-directory`)
- GitHub Actions rechazaba el workflow antes de ejecutar ningún job (error YAML: "not enough info")
- Reparado con Python: completado el step completo con `working-directory`, `env` (4 secrets) y `run: python sync_job.py $ARGS`
- Validado con `yaml.safe_load()` ✅
- Commit `c70d44b` → main (push directo por urgencia de CI)

### 2. Reparación de 6 archivos truncados (dev server roto)
El dev server mostraba `api.ts:497 Expected ";" but found ":"`. Causa: Write tool de sesiones anteriores había truncado archivos silenciosamente.

Archivos reparados (commit `3f095e1` → feature/demo-audit-mode):
- `src/features/seguimiento-fv/api.ts` — eliminado fragmento duplicado + truncación completada + 3× `(supabase as any)` para columnas post-migración
- `src/App.tsx` — route `/cartera-senior` truncada completada
- `src/components/layout/Sidebar.tsx` — etiquetas de cierre completadas
- `src/core/demo/fixtures.ts` — 221 líneas recuperadas desde `origin/feature/demo-audit-mode`
- `src/features/seguimiento-fv/components/PlantasTab.tsx` — truncación + `title` → `aria-label`
- `src/features/seguimiento-fv/components/ProduccionTab.tsx` — truncación + formatter recharts tipado

### 3. Diagnóstico Run #97
- Run #95 fallaba instantáneamente (YAML inválido) sin duración ni logs
- Run #97 (con fix): 44s, Python ejecuta, Playwright carga storage_state, Supabase responde
- Error real: `AUTH_REDIRECT` — Huawei invalida la sesión server-side aunque las cookies tienen fecha 2026-05-17
- Cookie `roarand` (CSRF) ausente en las cookies guardadas
- Script detectó correctamente el error y registró la credencial como inválida en Supabase

### 4. Commit módulo FV completo a feature/fv-operational-redesign
- Commit `7c64604` con 14 archivos, 3661 inserciones
- Archivos: todos los componentes seguimiento-fv, fixtures.ts, api.ts, Edge Function fv-create-credential, migración SQL 20260510, sync_job.py, extract_cookies.py
- Push a nueva rama `feature/fv-operational-redesign` (creada desde feature/demo-audit-mode en estado con todos los fixes)

## Qué quedó pendiente

- **Despliegue coordinado FV** (en orden estricto):
  1. Backup tablas fv en Supabase Dashboard
  2. Configurar `FV_ENCRYPTION_KEY` en Supabase Secrets
  3. `supabase functions deploy fv-create-credential`
  4. Aplicar `20260510_fv_alta_manual_credenciales.sql`
  5. `python sync_job.py --check-secrets`
  6. `extract_cookies.py` localmente (cookies frescas)
  7. Run FV Sync manual → primer sync real

- **CRM backlog**: SQL fase28.6, deploy chat-consultor, regenerar tipos Supabase

## Decisiones tomadas

- No extraer cookies antes del despliegue coordinado (evitar repetir tras migración)
- Los archivos truncados se detectan con Python bytes, no con Read tool (Read devuelve caché/git)
- El commit FV fue a `feature/fv-operational-redesign` (no a main) — despliegue cuando haya ventana controlada
