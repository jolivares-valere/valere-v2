# Sesión 2026-07-04 — FASE 2 · Telemetría y Auditoría de Enlaces (Cowork)

Workstream propio con auditoría externa (Fable 5) verificando contra BBDD y GitHub. Fase 1 (libro de ventas) corrió en paralelo sin compartir repo.

## Qué se hizo

**Telemetría viva (PR #55, squash 36b1f16 en main).** Reescrito `src/core/utils/telemetry.ts`: cola con flush a 20 eventos / 30 s / pagehide (fetch keepalive), INSERT REST directo a `client_telemetry` con Bearer token vivo (snapshot vía `onAuthStateChange` — enmienda E1: anon no tiene grants), dedupe anti-tormenta 5/firma, exclusión anti-bucle de la propia tabla, captura de 4xx/5xx + queries >3 s (decisión Juan) + errores JS + rejections + error boundaries + route_change 100% (decisión Juan) + web vitals. Integraciones: `global.fetch = telemetryFetch` en client.ts, `<TelemetryTracker/>` en App.tsx, `trackErrorBoundary` en ErrorBoundary. Verificación del auditor con evidencia positiva: 18+ eventos, user_id correcto, payloads sin PII, 400 real forzado con `/empresas/no-soy-un-uuid` (UUID no parseable → PostgREST 400; con UUID válido inexistente es 200-vacío por maybeSingle), error JS de test capturado.

**Inventario de enlaces (`docs/AUDITORIA_ENLACES_FASE2.md`, aprobado punto de parada 2).** 40 rutas × todos los Link/navigate/href/menú. Estáticamente sano. Hallazgos: H1 filtro `estado` ignorado en ContratosPage (el subagente Explore dijo "cero rotos"; el contraste manual lo cazó), H2 placeholder FV, H3 dimensión runtime pendiente de telemetría, H5 catch-all silencioso (auditor), H6 200-vacío esconde entidades rotas (auditor). Ampliaciones: A1 externos (limpio; riesgo residual en datos `pdf_url`), A2 contrato de query params (H1 era la clase entera).

**Arreglos H1+H5+H6 (PR #56, 61665b5 en main).** ContratosPage lee `estado` + chip con X; `NotFoundPage` (core/components) sustituye el catch-all y emite `custom {tipo:'ruta_no_encontrada'}`; `EntidadNoEncontrada` (core/components) en detalle de empresas, contratos y expedientes emite `custom {tipo:'entidad_no_encontrada'}`; `trackCustom()` en telemetry.ts. Docs RAG: `docs/help/general/pagina-no-encontrada.md`, `docs/help/contratos/filtrar-por-estado.md`. Verificado en preview (3 huellas visuales) + tabla (2 eventos custom).

## Incidentes y lecciones

- **H4 (crítico, resuelto):** el commit c809ef4 arrastró 2 líneas SIPS de App.tsx cuyo código estaba sin trackear → rama que compilaba en local pero no en CI. Fix 56577cb; prueba objetiva = build verde de Cloudflare. **Lección: `git diff origin/main -- <fichero>` antes de commitear ficheros compartidos; el add por fichero no protege de mezclas dentro de un fichero.** Aplicada como cortafuegos en el PR #56 con éxito.
- Sandbox no puede escribir `.git/` (locks huérfanos + sección config truncada); limpieza por PowerShell de Juan. El mount sirvió versiones stale (tsc del sandbox con falsos errores) — validación siempre en terminal de Juan.

## Pendiente

- Cierre de fase (tras 2-3 días de uso real): ranking de errores/ruta (H3), regenerar copia Drive del inventario (la actual no incluye H5/H6/A1/A2), informe de cierre 8 secciones en Drive/BACKUP CRM VALERE.
- Derivadas: E2 revocar DELETE/TRUNCATE de authenticated sobre client_telemetry (ventana BBDD, NO F2); SQL `pdf_url LIKE '%vercel%'`; botón "informar de un problema" (`reported_incident` libre en la CHECK).
- F1/SIPS: su material sigue en el árbol sin commitear; aviso en outbox 2026-07-04T23-30-00.

## Decisiones

Queries lentas >3s se capturan aunque den 200; route_change al 100%; PRs de una sola historia; merge solo tras verificación positiva del auditor contra BBDD.
