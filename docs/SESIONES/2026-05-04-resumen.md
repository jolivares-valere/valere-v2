# Sesión 2026-05-04 — Pre-uso real: smoke test + onboarding + feedback

**Agente:** Cowork (Claude Opus 4.7)
**Duración:** ~30 min
**Tipo:** validación + documentación (cero código nuevo)
**Frase guía aplicada:** *"El modelo de datos ya soporta el flujo real. Lo siguiente no es más código, es uso real con el equipo."* — ChatGPT, 2026-05-01

---

## Punto de partida

Ayer (2026-05-03) se cerraron 4 usuarios reales operativos en BD prod con funciones asignadas y se reasignaron las 3 oportunidades demo a sus responsables reales. Outbox dejaba instrucción explícita: NO añadir features, SÍ recoger feedback de uso real.

## Plan acordado al abrir

Cuatro bloques cortos, en orden de utilidad:
1. Smoke test BD + código de los 4 perfiles
2. Borradores de onboarding para los 3 nuevos
3. Mecanismo de feedback (markdown plantilla)
4. Verificación deploy Cloudflare

Juan dio luz verde con "arranca como planteas" → ejecuté 1+4 en paralelo, devolví resultado, Juan pidió continuar → ejecuté 2+3 → Juan pidió "termina lo previsto" → cierre de sesión.

## Trabajo realizado

### Verificación deploy
`GET https://valere-v2.pages.dev/login` → HTTP 200, último bundle servido. CSP correcto.

### Smoke test BD (objetivo)
Consulta SQL sobre `oportunidades` filtrando por `responsable_actual_id` para cada uno de los 4 usuarios. Resultado: cada user verá exactamente la oportunidad demo que le corresponde según el flujo (telemarketing → esperando_factura, analista → factura_recibida, asesor_senior → asignada_a_senior). Juan vacío (no es responsable de demos, supervisa).

### Auditoría Sidebar/App.tsx
- ✅ Sección "Captación" filtra correctamente por `funciones`: cada user solo ve su bandeja.
- ✅ `/admin` protegida por `roles: [master, manager]`.
- ⚠️ Sección "CRM Comercial" (13 items) y "Gestión de Potencias" (9 items) visibles a todos sin filtro.
- ⚠️ AuthGuard solo soporta filtro por `role` legacy, no por `funciones`. 45+ rutas sin guard.
- ⚠️ RLS permisivo: cualquier authenticated puede CRUD cualquier tabla.

### Veredicto
Sistema LISTO para uso real con el equipo. Las fugas son deuda conocida (capa A/B/D del backlog), severidad baja-media, no bloquean. Confianza en el equipo + feedback durante 1ª semana es la cobertura suficiente.

## Documentos creados

1. `docs/SMOKE_TEST_4_USERS_2026-05-04.md` — auditoría completa con tabla de bandejas, fugas detectadas y veredicto.
2. `docs/ONBOARDING_4_USERS_2026-05-04.md` — 3 borradores de mensaje (Carolina A, Carolina M, Antonio) + recordatorio operativo Juan.
3. `docs/FEEDBACK_USO_REAL.md` — plantilla para registrar fricciones, 6 categorías de tags, trigger de procesamiento (3+ entradas o ≥7 días).

Adicionalmente:
- `docs/ESTADO.md` actualizado con snapshot del cierre.
- `.cowork/outbox/2026-05-04T00-00-00-uso-real-arrancado.md` (siguiente sesión).

## Pendientes operativos (no código)

- Juan envía los 3 mensajes a Antonio, Carolina M, Carolina A.
- Juan considera asignarse uno de los demos para validar UI desde perfil senior.
- Equipo llena `FEEDBACK_USO_REAL.md` a medida que aparezcan fricciones.

## Pendientes de código (NO tocar todavía)

Solo desbloquearlos si feedback real lo justifica:
- Capa A backlog permisos: filtros sidebar + guards de ruta por `funciones`.
- Capa B backlog permisos: matriz RLS por entidad.
- Capa D backlog permisos: RPCs con check de función para acciones críticas.
- Cualquier feature nueva que no esté en `docs/ROADMAP_FUSION.md`.

## Decisiones tomadas

- No abrir Chrome para login real. La BD + código dieron evidencia objetiva suficiente. Si aparece fricción visual en uso real, entonces sí.
- No tocar RLS hoy. Documentado en backlog 2026-05-03.
- Onboarding por whatsapp/email coloquial, no formal. Confianza > candado técnico para esta semana.
