# Sesion 2026-06-02 — Fase 3: Tarifas Indexadas

## Que se hizo

**Backend (prod):**
- Migration `20260602_retailer_offers_indexado`: nuevos campos `price_type` ('fijo'|'indexado') y `spread_eur_kwh` en tabla `comercializadora_ofertas`. Vista `retailer_offers` actualizada para exponerlos.

**Tipos TS:**
- `RetailerOffer` en `src/types/database.ts` con `price_type` y `spread_eur_kwh`

**Motor de calculo:**
- `SimulationParams.poolPrecioMedioEurKwh` (opcional)
- `calculateSimulatedInvoice`: si `offer.price_type === 'indexado'`, usa `(precio pool promedio + spread)` en lugar de precios fijos por periodo

**Hook nuevo:**
- `src/core/hooks/usePoolPrecioMedio.ts`: consulta `precios_pool_horarios` indicador 600 (OMIE), calcula media EUR/kWh para un rango de fechas

**Integracion AnalisisPage:**
- Calcula rango de fechas desde las facturas cargadas
- Llama a `usePoolPrecioMedio` y pasa el resultado al motor para ofertas indexadas

**UI AdminPage:**
- Formulario de ofertas: selector `fijo`/`indexado` + campo `spread_eur_kwh` (visible solo cuando indexado)
- `buildEmptyForm` y `startEditOffer` actualizados con los nuevos campos

## TSC
0 errores al cerrar sesion.

## Pendiente
- Widget `PrecioPoolCard` en Dashboard (mostrar precio pool actual)
- Tests `usePoolPrecioMedio` + rama indexada en calculator
- Sprint Carolina frontend: `CaptacionPage` toggle vista tabla + tab Enviados
- Commit de esta sesion: ejecutar PowerShell
