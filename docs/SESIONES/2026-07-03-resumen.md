# SesiÃ³n 2026-07-03 (Cowork) â€” SIPS F1: Buscador de CUPS cableado + EF desplegada

## QuÃ© se hizo
- Rebuild LIMPIO de F1 desde origin/main en rama `claude/f1-sips-cups` (se descarta la rama opaca `claude/sips-f1`).
- Copiados los 3 archivos SIPS del backup `.claude\...\f1_sips`:
  - `supabase/functions/resolver-sips-cups/index.ts` (EF orquestadora sobre datadis-proxy)
  - `src/features/sips/api.ts` (tipos, `resolverSipsCups`, `useResolverSips`, `sipsToAutofill`)
  - `src/features/sips/BuscadorCupsPage.tsx` (pantalla)
- Cableado: import + ruta `/buscador-cups` en `App.tsx`; Ã­tem Â«Buscador CUPSÂ» en `Sidebar` (crmItems); whitelist `/buscador-cups` para `asesor_senior` en `permissions.ts`.
- Doc RAG nueva `docs/help/datadis/buscador-cups.md`.
- Confirmado `datadis-proxy` desplegada (ACTIVE v13). Desplegada `resolver-sips-cups` en Supabase.

## QuÃ© quedÃ³ pendiente
- BotÃ³n "Traer de Datadis" (autorrelleno con `sipsToAutofill`): DIFERIDO a "Suministros iteraciÃ³n 2". Motivo: el CRM comercial aÃºn no tiene formulario de alta/ediciÃ³n de CUPS (Suministros solo lee `cups`). El Ãºnico sitio que hoy crea CUPS es Potencias `NuevoExpedienteModal`.

## Decisiones
- Rebuild desde main en vez de continuar la rama WIP `claude/sips-f1` (no inspeccionable desde sandbox) â†’ PR determinista.
- Autorrelleno se pospone hasta que exista el form de CUPS comercial.